{
		"name": "8e",
		"version": "2.0.5",
		"scripts": {
				"n": "git add . && git commit -m 0 && npx np patch --no-cleanup --no-tests"
		},
		"main": "mod.ts",
		"publishConfig": {
				"registry": "https://registry.npmjs.org"
		}
}
